{
  "version": "2.0.0",
  "tasks": [
    {
      "type": "process",
      "label": "docker compose up",
      "command": "docker",
      "args": ["compose", "up","--build", "-d"],
      "options": {
        "cwd": "${workspaceFolder}"
      }
    }
  ]
}
